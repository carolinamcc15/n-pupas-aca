package com.npupas.api.enums;


// Enum for action identifier in format Resource:Operation:atom
// i.e. Sales:View:History
public enum ActionIdentifier {
    ;

    ActionIdentifier(String name){
    }
}
